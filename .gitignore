# Output of the ONE PSIM Prototype Kit when it is pointed at prototypes/schedule_v6.html:
# `npm run check` writes its screenshots next to the prototype. Keep the curated set
# in screenshots/ instead — see README, section «screenshots/».
prototypes/shots/

# Single-file builds; regenerated by `npm run check … --inline-remote-fonts`
dist/

# OS and editor noise
Thumbs.db
desktop.ini
.DS_Store
