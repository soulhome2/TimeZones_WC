# Анализ работоспособности прототипа `schedule_v3.html` на реальных типах графиков

**Продукт:** ITV/Axxon — Интеллект X · Axxon One · Axxon Next
**Объект анализа:** `schedule_v3.html` (итоговый прототип экрана «Временные зоны»)
**Дата:** 08.09.2026
**Метод:** сценарии прогнаны через движок прототипа (расчёт часов, покрытие по дням,
предпросмотр payload, рендер таблицы исключений). Все числа в документе — измеренные,
а не оценочные.

---

## 1. Функционал работы с временными зонами, покрытый прототипом

Кратко, что прототип умеет делать с временными зонами как с объектами.

**Объект «расписание» (временная зона)**

| Операция | Реализация |
|---|---|
| Создание | `+ Новое расписание` (пустая неделя) или применение одного из 8 шаблонов |
| Копирование | `Дублировать` в меню расписания — с новым идентификатором и сброшенными связями |
| Переименование | Поле имени в шапке редактора |
| Удаление | С модальным подтверждением и перечнем зависимых объектов; отменяется через `Ctrl+Z` |
| Поиск / выбор | Поиск по имени, список с мини-превью недели, часами за неделю и счётчиком использований |
| Сохранение | `Сохранить` / `Отмена`, индикатор несохранённых изменений, защита при переключении расписания |
| Обмен | Импорт / экспорт JSON, предпросмотр запроса к API |

**Объект «интервал» внутри недельной сетки**

| Операция | Реализация |
|---|---|
| Создание | Рисование протяжкой по дорожке, `+ Добавить интервал`, быстрые заготовки (`Весь день`, `09:00–18:00`, `22:00–06:00`) |
| Изменение | Перетаскивание блока, растягивание за края, точные поля `С` / `По`, клавиши `←`/`→` и `Shift`+`←`/`→` |
| Удаление | Стирание протяжкой, `Del`, кнопка удаления в панели дня, `Очистить` день / неделю / отмеченные дни |
| Шаг привязки | 5 / 15 / 30 / 60 мин |
| Через полночь | `start + len > 24 ч` — одна сущность, продолжение видно в следующем дне |
| Цикличность | «N мин каждые M мин» на интервал |
| История | Undo / Redo, 60 шагов |

**Массовые операции по дням недели**

Чекбоксы дней задают область действия для шаблонов, инверсии и очистки. Копирование дня в
выбранные дни в режиме **замены** или **добавления**, копирование одного интервала,
инверсия дня или недели.

**Календарные надстройки (объявлены в UI)**

Даты-исключения с режимами «нерабочий», «активно 24 часа», «как в день недели», «свои
интервалы», повтор ежегодно, комментарий; флаг общесерверного списка праздников; срок
действия `Действует с` / `Действует по`; периодичность «повторять каждые N дней».

> ⚠️ Именно эта, четвёртая группа — источник почти всех провалов, разобранных ниже:
> поля редактируются и сохраняются в модель, но **не участвуют ни в одном расчёте,
> не отображаются на сетке и не попадают в payload API**.

---

## 2. Легенда таблиц

| Знак | Значение |
|:--:|---|
| ✅ | Работает штатными средствами, результат виден и посчитан |
| 🟡 | Работает частично: либо теряется часть смысла, либо результат нельзя проверить в интерфейсе |
| 🧩 | Объявлено в UI, данные сохраняются, но на поведение и на API не влияет |
| ⛔ | Выразить нельзя |

Колонки **Созд. / Изм. / Удал.** — возможность создать, изменить и удалить конфигурацию
такого типа.

---

## 3. Базовые недельные графики

| Сценарий | Созд. | Изм. | Удал. | Итог | Комментарий |
|---|:--:|:--:|:--:|:--:|---|
| **5/2**, Пн–Пт 09:00–18:00 | ✅ | ✅ | ✅ | ✅ | Шаблон «Рабочие часы». Измерено: **45 ч/нед**, 5 интервалов в payload |
| 5/2 с сокращённой пятницей | ✅ | ✅ | ✅ | ✅ | Пятница правится отдельно, остальные дни копированием |
| Расширенный офис Пн–Пт 07:00–21:00 | ✅ | ✅ | ✅ | ✅ | Шаблон «Расширенный офис» |
| Обеденный перерыв (09:00–13:00 + 14:00–18:00) | ✅ | ✅ | ✅ | ✅ | Два несмежных интервала в дне; не сливаются, т.к. между ними разрыв |
| **Дневная смена** 08:00–20:00 | ✅ | ✅ | ✅ | ✅ | Обычный интервал |
| **Ночная смена** 22:00–06:00 | ✅ | ✅ | ✅ | ✅ | Одна сущность через полночь. Измерено: **56 ч/нед**, покрытие **8 ч** в каждом дне |
| Круглосуточно 24/7 | ✅ | ✅ | ✅ | ✅ | Шаблон «24/7» |
| Только выходные (Сб–Вс) | ✅ | ✅ | ✅ | ✅ | Шаблон «Выходные» |
| Нерабочее время (Пн–Пт 18:00–09:00 + выходные) | ✅ | ✅ | ✅ | ✅ | Шаблон «Нерабочее время» или кнопка `Инвертировать` |
| Цикличная выборка (5 мин каждые 30 мин) | ✅ | ✅ | ✅ | ✅ | Часы считаются с учётом доли импульса |
| Разные окна в каждый день недели | ✅ | ✅ | ✅ | ✅ | Ограничений на число интервалов в дне нет |

**Вывод по разделу:** всё, что укладывается в **семидневный цикл**, прототип закрывает
полностью — включая переход через полночь и цикличность.

---

## 4. Сменные графики

| Сценарий | Созд. | Изм. | Удал. | Итог | Комментарий |
|---|:--:|:--:|:--:|:--:|---|
| **Две смены в сутки** 06:00–14:00 + 14:00–22:00 | 🟡 | 🟡 | ✅ | 🟡 | Шаблон даёт 2 блока, но **любая правка дня сливает смежные интервалы в один 06:00–22:00**. Измерено: после `setDay` в понедельнике 1 блок вместо 2. Часы сохраняются (80 ч/нед), граница смен теряется |
| Три смены в сутки 08/08/08 | 🟡 | 🟡 | ✅ | 🟡 | То же слияние смежных интервалов |
| Дневная и ночная смены **как отдельные зоны** | ✅ | ✅ | ✅ | ✅ | Два расписания, по одному на бригаду — рабочий обходной путь |
| **2/2** (цикл 4 суток) | ⛔ | ⛔ | — | ⛔ | Сетка жёстко недельная. Поле «Повторять каждые N дней» инертно: измерено — не влияет на часы, не меняет payload, отсутствует в запросе |
| **3/3**, **4/4**, **4/3** | ⛔ | ⛔ | — | ⛔ | То же |
| Вахта 15/15, 30/30 | ⛔ | ⛔ | — | ⛔ | То же |
| Чередование недель (неделя А / неделя Б) | ⛔ | ⛔ | — | ⛔ | Цикл 14 суток, недельной сеткой не выражается |
| Несколько бригад по одному циклу со сдвигом | ⛔ | ⛔ | — | ⛔ | Нет понятия «сдвиг цикла» и «бригада» |
| Скользящие выходные при 5/2 | ⛔ | ⛔ | — | ⛔ | Требует цикла, не кратного неделе |

**Отдельно о поле «Повторять каждые N дней».** Подсказка в интерфейсе обещает «сменные
графики вида 2/2 или 4/4», но заявленная семантика — «повторяет **строку понедельника**
каждые N дней» — принципиально не может выразить два подряд рабочих дня в четырёхдневном
цикле: получится 1 рабочий и 3 выходных. То есть поле не только инертно технически, но и
описано механикой, которая нужную задачу не решает даже в теории.

**Вывод по разделу:** графики с циклом, не кратным семи суткам, **прототипом не
поддерживаются**. Это самый крупный функциональный провал.

---

## 5. Праздники и особые даты

| Сценарий | Созд. | Изм. | Удал. | Итог | Комментарий |
|---|:--:|:--:|:--:|:--:|---|
| Праздник — нерабочий день | 🧩 | 🧩 | ✅ | 🧩 | Строка исключения с режимом «нерабочий». Данные сохраняются, но на сетке не видны, в часах не учтены, в payload не уходят |
| Праздник с повтором ежегодно | 🧩 | 🧩 | ✅ | 🧩 | Флаг «ежегодно» есть; вычисления по нему не производятся |
| Диапазон праздников (новогодние каникулы) | 🧩 | 🧩 | ✅ | 🧩 | Диапазон `с` / `по`, охватывает все дни внутри |
| Праздник = «как в воскресенье» | 🧩 | 🧩 | ✅ | 🧩 | Выбор **одного** дня недели |
| **Сокращённый предпраздничный день** | 🟡 | 🟡 | ✅ | 🟡 | Режим «свои интервалы» редактирует **только один интервал** на дату. Измерено: 2 поля времени на строку. «09:00–13:00 + 14:00–17:00» на одну дату не выразить |
| Общесерверный список праздников | 🧩 | 🧩 | — | 🧩 | Флаг есть, самого списка и его влияния нет |
| Плановое обслуживание на конкретную дату | 🧩 | 🧩 | ✅ | 🧩 | Тот же механизм исключений |
| День перехода на летнее время | 🟡 | 🟡 | ✅ | 🟡 | Есть текстовое пояснение и возможность завести исключение; автоматики нет |
| Исключение на набор дней недели | ⛔ | ⛔ | — | ⛔ | Режим «как в день недели» принимает только один день |
| Приоритет при пересечении исключений | ⛔ | ⛔ | — | ⛔ | Пересечения диапазонов не проверяются, порядок применения не определён |

**Вывод по разделу:** праздники **описываются, но не действуют**. Как форма ввода раздел
готов; как работающая механика — нет.

---

## 6. Сложные календарные конфигурации

| Сценарий | Созд. | Изм. | Удал. | Итог | Комментарий |
|---|:--:|:--:|:--:|:--:|---|
| **Месяц, где почти каждый день свой график** | 🟡 | 🟡 | 🟡 | 🟡 | Механически возможно: 30 строк исключений. Измерено: payload не изменился (5 интервалов до и после добавления 30 исключений), часы остались 45 ч/нед |
| Проверить получившийся месяц глазами | ⛔ | — | — | ⛔ | Календарного представления нет, сетка всегда 7 строк |
| Размножить одну особую дату на другие даты | ⛔ | ⛔ | — | ⛔ | Копирования исключений нет (для дней недели копирование есть) |
| Хронологический порядок особых дат | ⛔ | — | — | ⛔ | Измерено: таблица рендерится **в порядке добавления**. Даты 31.12 → 09.01 → 12.06 → 08.03 остаются в этом порядке |
| Массовое удаление особых дат | 🟡 | — | 🟡 | 🟡 | Только по одной строке; кнопки «очистить все исключения» нет |
| Комбинация двух расписаний (основное + корректирующее) | ⛔ | ⛔ | — | ⛔ | Десктопная возможность заменена исключениями; самой комбинации нет |
| Сезонный график (лето / зима) | 🧩 | 🧩 | ✅ | 🧩 | Через `Действует с` / `Действует по` и два расписания, но поля инертны |

**Трудозатраты на «месяц по дням».** На одну дату: добавить строку, задать дату «с», дату
«по», режим, время начала, время окончания — **6 действий**. На 30 дат — около **180
действий** без возможности копирования и без визуального контроля результата.

**Вывод по разделу:** формально сценарий выполним, практически — неработоспособен:
нельзя ни проверить результат, ни ускорить ввод, ни задать больше одного интервала на дату.

---

## 7. Дефекты, обнаруженные при проверке

| № | Дефект | Как проявляется | Критичность |
|:--:|---|---|:--:|
| 1 | Исключения, срок действия и периодичность **отсутствуют в payload** | Добавлено 30 исключений — предпросмотр запроса не изменился (5 интервалов до и после). «Исполняемая спецификация» описывает только недельный шаблон | Высокая |
| 2 | Расчёты игнорируют исключения | Расписание 24/7 с исключением «нерабочий на сегодня» показывает пилюлю **«Активно сейчас»**. Часы за неделю и % покрытия тоже не учитывают особые даты | Высокая |
| 3 | Поле «Повторять каждые N дней» инертно и описано неверно | Не влияет ни на сетку, ни на расчёты, ни на API; заявленная механика не выражает 2/2 даже теоретически | Высокая |
| 4 | Перенос через полночь не ограничен сутками | Интервал 20:00 длиной 30 ч даёт покрытие **26 ч** в одних календарных сутках | Средняя |
| 5 | Смежные интервалы сливаются без предупреждения | Две смены 06–14 и 14–22 превращаются в 06–22 при первой же правке дня | Средняя |
| 6 | Таблица исключений не сортируется по дате | При вводе вразнобой порядок остаётся хаотичным, ошибки не видны | Низкая |
| 7 | Нет проверки пересечения диапазонов исключений | Две строки на одну дату с разными режимами — поведение не определено | Средняя |

---

## 8. Краткие выводы о работоспособности

**Что прототип действительно закрывает.** Всё, что описывается **семидневным циклом**:
5/2, любые дневные окна, обеденные перерывы, ночные смены через полночь, круглосуточную
работу, выходные, инверсию в «нерабочее время» и цикличную выборку. Здесь создание,
изменение и удаление работают полноценно, результат виден и посчитан, история правок
восстанавливается. Механика ввода — рисование протяжкой, привязка к шагу, копирование
дней, область действия по чекбоксам — быстрая и предсказуемая. Для типового объекта
(офис, магазин, склад с одной сменной парой) прототип **пригоден как есть**.

**Где он перестаёт работать.** Ровно на границе недельного цикла. Сменные графики 2/2,
3/3, 4/4, вахта, чередование недель, скользящие выходные — **не выражаются никак**.
Поле «Повторять каждые N дней» создаёт ложное впечатление поддержки: оно редактируется,
сохраняется, отображается как «настроено», но не влияет ни на что и описано механикой,
которая задачу не решает.

**Главный риск — не отсутствие функций, а расхождение между заявленным и работающим.**
Разделы «Исключения и праздники» и «Срок действия, периодичность и дополнительно»
выглядят полностью готовыми: поля заполняются, счётчики в заголовках аккордеонов растут
(«2 правила», «настроено»), данные переживают сохранение. Но ни одно из этих значений не
доходит ни до расчётов, ни до API. Пользователь на демонстрации заведёт праздники,
увидит подтверждение — и получит на объекте запись в архив по обычному графику. Для
прототипа, который позиционируется как исполняемая спецификация, это опаснее, чем честно
отсутствующий раздел.

**Итоговая оценка по типам графиков.** Из **37** проверенных сценариев:

| Итог | Сценариев | Доля |
|---|:--:|:--:|
| ✅ работает полностью | **12** | 32 % |
| 🟡 работает частично | 6 | 16 % |
| 🧩 объявлено, но не действует | 7 | 19 % |
| ⛔ выразить нельзя | **12** | 32 % |

Иначе говоря, прототип уверенно решает задачи одного объекта с недельным графиком и не
решает задачи предприятия со сменными бригадами и насыщенным производственным календарём.
Обратите внимание на соотношение: доля «работает полностью» равна доле «невозможно»,
а ещё треть сценариев находится в серой зоне между ними.

---

## 9. Что нужно закрыть, чтобы прототип покрыл заявленные сценарии

Приоритеты выставлены по соотношению «влияние на работоспособность / объём переделок».

| Приоритет | Задача | Почему именно так |
|:--:|---|---|
| **P0** | Довести исключения, срок действия и периодичность **до payload и до расчётов** либо честно убрать их из UI | Сейчас это главный источник ложных ожиданий. Любой из двух путей лучше текущего состояния |
| **P0** | Заменить «Повторять каждые N дней» на **редактор цикла произвольной длины**: N строк-суток вместо 7 фиксированных, с привязкой начала цикла к дате | Единственный способ закрыть 2/2, 3/3, 4/4 и вахту. Сетка и вся механика рисования переиспользуются как есть — меняется только число строк и их подписи |
| **P1** | **Календарное представление на месяц** с раскраской по итоговому графику (недельный шаблон + исключения + праздники) | Без него сложные конфигурации невозможно проверить, а значит и доверять им |
| **P1** | Разрешить **несколько интервалов на дату-исключение** (сейчас редактируется только первый) | Закрывает сокращённые предпраздничные дни с перерывом |
| **P1** | Ограничить перенос через полночь сутками в расчёте покрытия | Дефект №4, правка локальная |
| **P2** | Сортировка исключений по дате, проверка пересечений, копирование и массовое удаление строк | Делает «месяц по дням» практически выполнимым |
| **P2** | Не сливать смежные интервалы молча: либо сохранять границу смен, либо предупреждать | Зависит от продуктового решения: модель описывает покрытие или смены |
| **P3** | Понятие «бригада» и сдвиг цикла между бригадами | Нужно, если продукт претендует на промышленный сменный учёт |

---

## Ссылки

- `schedule_v3.html` — анализируемый прототип
- `TimeZones_Prototype_v3.MD` — отличия от предыдущих версий, открытые вопросы по API
- `Schedule_prototypes_analysis.md` — разбор исходных вариантов и матрица покрытия
- [Axxon One 3.0 — Time zones](https://docs.axxonsoft.com/confluence/spaces/ONE2025/pages/314537999/Time+zones)
